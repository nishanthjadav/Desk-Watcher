{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Desk Watcher main window.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
